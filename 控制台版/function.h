#pragma once
namespace function {
	int decrypt(int address);
	void encrypt(INT32 Address, INT32 Value);
	int getGameStatus();
	POS getCurrentRoomPos();
	POS getBossRoomPos();
	bool isBossRoom();
	bool isOpenDoor();
}
